@model IEnumerable<Server.Entities.DesafioColorado.TipoTelefoneViewModel>
@{
    ViewBag.Title = "Tipo Telefone";
}


<div id="view-all">
     <div class="col-md-8 offset-md-2">

        <h1 class="text-center"><i class="fas fa-comments-dollar text-success"></i> Cadastro de Telefone</h1>


        <table class="table">
            <thead class="thead-light">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.CodigoTipoTelefone)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DescricaoTelefone)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DataInsercao)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.UsuarioInsercao)
                    </th>
                    <th>
                        <a onclick="showInPopup('@Url.Action("AddOrEdit","TipoTelefone",null,Context.Request.Scheme)','Novo Cadastro Tipo Telefone')" class="btn btn-success text-white"><i class="fas fa-random"></i> Novo Cadastro</a>
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.CodigoTipoTelefone)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DescricaoTelefone)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.DataInsercao)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.UsuarioInsercao)
                        </td>
                        <td>
                            <div>
                                <a onclick="showInPopup('@Url.Action("AddOrEdit","TipoTelefone",new {id=item.CodigoTipoTelefone},Context.Request.Scheme)','Update Transaction')" class="btn btn-info text-white"><i class="fas fa-pencil-alt"></i> Edit</a>
                                <form asp-action="Delete" asp-route-id="@item.CodigoTipoTelefone" onsubmit="return jQueryAjaxDelete(this)" class="d-inline">
                                    <input type="hidden" asp-for="@item.CodigoTipoTelefone" />
                                    <input type="submit" value="Delete" class="btn btn-danger" />
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="modal" tabindex="-1" role="dialog" id="form-modal">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title"></h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
            </div>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}